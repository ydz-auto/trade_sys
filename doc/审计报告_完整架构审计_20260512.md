# 交易 Agent 系统 - 架构审计报告

**审计日期**: 2026-05-12  
**审计范围**: 整个后端系统架构、模块完整性、数据流集成

---

## 一、系统架构概览

### 1.1 已实现模块

| 模块 | 状态 | 说明 |
|------|------|------|
| **shared** | ✅ 完成 | 公共协议（StandardEvent）、配置、状态管理 |
| **infrastructure** | ✅ 完成 | 熔断/重试/降级、消息队列、数据库、缓存、日志、监控 |
| **data_service** | ✅ 基础完成 | 数据采集服务（含新实时源 QQ/Telegram） |
| **event_service** | ⚠️ 部分完成 | 事件提取服务（仅基础实现） |
| **fusion_service** | ⚠️ 部分完成 | 信号融合服务（仅基础实现） |
| **strategy_service** | ⚠️ 部分完成 | 策略服务（仅基础实现） |
| **approval_service** | ✅ 完成 | 审批服务（决策门、人工审批） |
| **llm_service** | ✅ 完成 | LLM 服务（LLM 池、新闻提取、情绪分析） |

### 1.2 数据流架构

```
┌─────────────────────────────────────────────────────────────────┐
│  数据源 (QQ/Telegram/Binance/etc)                               │
└─────────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────────┐
│  data_service (数据服务)                                         │
│  ├─ sources (实时源) ← 新增加的 QQ/Telegram 实时源              │
│  ├─ collectors (定时采集) ← 历史遗留采集器                      │
│  ├─ event_bus (事件总线) ← 已实现但未完全集成                   │
│  ├─ kafka_producer (Kafka 生产者) ← 已实现                      │
│  └─ storage (数据存储) ← 已实现 (ClickHouse)                   │
└─────────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────────┐
│  Kafka (消息总线)                                               │
│  ├─ tradeagent.raw_data (原始数据)                             │
│  ├─ tradeagent.events (事件)                                   │
│  ├─ tradeagent.features (特征)                                 │
│  ├─ tradeagent.signals (信号)                                  │
│  └─ tradeagent.orders (订单)                                   │
└─────────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────────┐
│  处理服务                                                       │
│  ├─ event_service (事件服务) ← 仅基础实现                       │
│  ├─ fusion_service (融合服务) ← 仅基础实现                     │
│  ├─ strategy_service (策略服务) ← 仅基础实现                   │
│  ├─ [MISSING] risk_service (风控服务) ← 未实现                 │
│  └─ [MISSING] execution_service (执行服务) ← 未实现             │
└─────────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────────┐
│  approval_service (审批服务) ← 已实现                           │
└─────────────────────────────────────────────────────────────────┘
```

---

## 二、详细模块审计

### 2.1 shared 模块 - ✅ 良好

| 文件 | 状态 | 说明 |
|------|------|------|
| `shared/contracts/__init__.py` | ✅ 完成 | StandardEvent 统一协议，内部/外部合约分离清晰 |
| `shared/config/` | ✅ 完成 | 配置管理器，默认配置完整 |
| `shared/state/` | ✅ 完成 | 系统状态管理 |
| `shared/tdp/` | ✅ 完成 | TDP 适配器 |

### 2.2 infrastructure 模块 - ✅ 良好

| 模块 | 状态 | 说明 |
|------|------|------|
| `infrastructure/resilience/` | ✅ 完成 | 熔断、重试、降级，已修复导出问题 |
| `infrastructure/messaging/` | ✅ 完成 | Kafka 生产者/消费者封装 |
| `infrastructure/database/` | ✅ 完成 | PostgreSQL + ClickHouse 支持 |
| `infrastructure/cache/` | ✅ 完成 | Redis 缓存管理器 |
| `infrastructure/logging/` | ✅ 完成 | 统一日志系统 |
| `infrastructure/monitoring/` | ✅ 完成 | 监控和告警 |
| `infrastructure/webhook/` | ✅ 完成 | Webhook 接收器 |
| `infrastructure/websocket/` | ✅ 完成 | WebSocket 服务器 |
| `infrastructure/api_gateway/` | ✅ 完成 | API 网关框架 |

### 2.3 data_service 模块 - ✅ 基础完成，⚠️ 部分需要集成

#### 2.3.1 新增加的实时源 (sources/) - ✅ 完成

| 文件 | 状态 | 说明 |
|------|------|------|
| `sources/base.py` | ✅ 完成 | BaseSource 基类，统一优先级、去重、情绪分析、事件构建 |
| `sources/qq_realtime.py` | ✅ 完成 | QQ 实时源 (NapCatQQ + WebSocket) |
| `sources/telegram_realtime.py` | ✅ 完成 | Telegram 实时源 (Telethon) |
| `sources/__init__.py` | ✅ 完成 | 统一导出 |

**关键功能**：
- ✅ P0/P1/P2 关键词优先级过滤
- ✅ 自动去重机制
- ✅ 情绪分析 (bullish/bearish/neutral)
- ✅ 币种提取 (BTC/ETH/SOL/...)
- ✅ 标准事件 (StandardEvent) 自动构建
- ✅ 模拟模式 (无需配置即可测试)

**⚠️ 问题**：实时源尚未集成到主流程中！

#### 2.3.2 历史采集器 (collectors/) - ✅ 保留用于兼容

| 文件 | 状态 | 说明 |
|------|------|------|
| `collectors/base_collector.py` | ✅ 完成 | 基类采集器，含熔断/重试/降级 |
| `collectors/exchange_collector.py` | ✅ 完成 | 交易所价格采集 |
| `collectors/etf_collector.py` | ✅ 完成 | ETF 数据采集 |
| `collectors/news_collector.py` | ✅ 完成 | 新闻采集 (含 LLM 支持) |
| `collectors/news_feed_collector.py` | ✅ 完成 | RSS 采集 |
| `collectors/news_api_collector.py` | ✅ 完成 | API 采集 (CryptoPanic 等) |
| `collectors/news_hub.py` | ✅ 完成 | 新闻中心聚合 |
| `collectors/macro_collector.py` | ✅ 完成 | 宏观数据采集 |
| `collectors/social_media_collector.py` | ✅ 完成 | 社交媒体采集 |
| `collectors/trader_collector.py` | ✅ 完成 | 交易员观点采集 |
| `collectors/crypto_stock_collector.py` | ✅ 完成 | 加密股票采集 |
| `collectors/llm_scraper.py` | ✅ 完成 | LLM 爬虫 |
| `collectors/binance_websocket.py` | ⚠️ 部分完成 | Binance WebSocket，需要完善 |
| `collectors/telegram_adapter.py` | ⚠️ 已过时 | 历史遗留，建议删除，使用 sources/telegram_realtime |
| `collectors/multi_source.py` | ✅ 完成 | 多源采集器 |

#### 2.3.3 适配器 (adapters/) - ✅ 完成

| 文件 | 状态 | 说明 |
|------|------|------|
| `adapters/skill_adapter.py` | ✅ 完成 | Skill 适配器（将各种源转换为 StandardEvent） |
| `adapters/qq_adapter.py` | ⚠️ 已过时 | 历史遗留，建议删除，使用 sources/qq_realtime |
| `adapters/cryptopanic_adapter.py` | ✅ 完成 | CryptoPanic 适配器 |
| `adapters/whale_alert_adapter.py` | ✅ 完成 | Whale Alert 适配器 |
| `adapters/twitter_adapter.py` | ✅ 完成 | Twitter 适配器 |

#### 2.3.4 事件总线 (event_bus/) - ✅ 已实现但未集成

| 文件 | 状态 | 说明 |
|------|------|------|
| `event_bus/event_bus.py` | ✅ 完成 | EventBus 事件总线，支持订阅/发布、过滤、历史回放 |
| `event_bus/__init__.py` | ✅ 完成 | 统一导出 |

**⚠️ 问题**：EventBus 已实现，但尚未与各服务集成！

#### 2.3.5 数据流水线 (pipeline/) - ✅ 部分实现

| 文件 | 状态 | 说明 |
|------|------|------|
| `pipeline/scheduler.py` | ✅ 完成 | 任务调度器 |
| `pipeline/realtime_push.py` | ✅ 完成 | 实时推送 |
| `pipeline/readhub_pipeline.py` | ✅ 完成 | ReadHub 完整流水线 |
| `pipeline/__init__.py` | ✅ 完成 | 统一导出 |

#### 2.3.6 主入口

| 文件 | 状态 | 说明 |
|------|------|------|
| `main.py` (FastAPI) | ✅ 完成 | REST API + WebSocket 服务器 |
| `main_kafka.py` | ✅ 完成 | Kafka 生产者模式 |
| `twitter_push_server.py` | ✅ 完成 | Twitter 推送服务器 |
| `tdp_adapter.py` | ✅ 完成 | TDP 协议适配器 |
| `storage.py` | ✅ 完成 | ClickHouse 存储 |
| `cache.py` | ✅ 完成 | 缓存层 |

### 2.4 event_service 模块 - ⚠️ 基础完成

| 文件 | 状态 | 说明 |
|------|------|------|
| `main_kafka.py` | ✅ 完成 | Kafka 消费者，消费 raw_data 并发布 events |
| `mapper.py` | ✅ 完成 | 事件类型映射 |
| `services/event_extractor.py` | ⚠️ 简单实现 | 事件提取器（仅简单规则） |
| `consumers/raw_data_consumer.py` | ⚠️ 基础实现 | 原始数据消费者 |
| `producers/event_producer.py` | ⚠️ 基础实现 | 事件生产者 |
| `schemas/event_schema.py` | ✅ 完成 | 事件 Schema |

**⚠️ 问题**：
- 事件提取过于简单（仅关键词匹配），建议集成 LLM 服务进行更智能的事件提取
- 尚未与 data_service 的 EventBus 集成

### 2.5 fusion_service 模块 - ⚠️ 基础完成

| 文件 | 状态 | 说明 |
|------|------|------|
| `main_kafka.py` | ✅ 完成 | Kafka 消费者，消费 events 并发布 signals |
| `engine.py` | ✅ 完成 | 融合引擎（聚合、评分） |
| `aggregator.py` | ✅ 完成 | 事件聚合器 |
| `buffer.py` | ✅ 完成 | 事件缓冲区 |
| `scorer.py` | ✅ 完成 | 信号评分器 |
| `schemas/signal_schema.py` | ✅ 完成 | 信号 Schema |

### 2.6 strategy_service 模块 - ⚠️ 基础完成

| 文件 | 状态 | 说明 |
|------|------|------|
| `main_kafka.py` | ✅ 完成 | Kafka 消费者，消费 signals 并做决策 |
| Dockerfile | ✅ 完成 | Docker 镜像文件 |
| requirements.txt | ✅ 完成 | 依赖文件 |

**⚠️ 问题**：
- 策略逻辑过于简单（仅基于置信度）
- 尚未实现因子系统
- 尚未实现 Regime 引擎

### 2.7 approval_service 模块 - ✅ 完成

| 文件 | 状态 | 说明 |
|------|------|------|
| `main.py` | ✅ 完成 | 审批服务主入口 |
| `decision_gate.py` | ✅ 完成 | 决策门（Auto/Manual/Hybrid 三种模式） |
| `telegram_bot.py` | ✅ 完成 | Telegram 通知机器人 |
| `status.py` | ✅ 完成 | 审批状态管理 |

### 2.8 llm_service 模块 - ✅ 完成

| 文件 | 状态 | 说明 |
|------|------|------|
| `llm/llm_pool.py` | ✅ 完成 | LLM 连接池（OpenAI/Anthropic/Minimax） |
| `services/news_extractor.py` | ✅ 完成 | 新闻提取服务 |
| `services/sentiment_analyzer.py` | ✅ 完成 | 情绪分析服务 |
| `services/social_analyzer.py` | ✅ 完成 | 社交分析服务 |
| `main.py` | ✅ 完成 | LLM 服务 API 入口 |
| `config.py` | ✅ 完成 | 配置文件 |
| `cache/` | ✅ 完成 | LLM 响应缓存 |
| `tests/` | ✅ 完成 | 测试文件 |

---

## 三、缺失功能清单 (按优先级)

### 🔴 P0 - 核心缺失（必须实现）

| 功能 | 影响 | 说明 |
|------|------|------|
| **实时源集成到主流程** | 🔴 高 | QQ/Telegram 实时源尚未与 data_service 主流程集成，数据无法流入 Kafka |
| **execution_service (执行服务)** | 🔴 高 | 缺少订单执行引擎，无法连接交易所下订单 |
| **risk_service (风控服务)** | 🔴 高 | 缺少风险控制服务（仓位管理、止损止盈、最大回撤等） |
| **EventBus 完整集成** | 🔴 高 | EventBus 已实现，但尚未与各服务集成 |

### 🟡 P1 - 重要缺失（建议实现）

| 功能 | 影响 | 说明 |
|------|------|------|
| **回测系统** | 🟡 中 | 缺少历史数据回测系统，无法验证策略效果 |
| **因子系统 (Factor Service)** | 🟡 中 | `doc/` 中有设计文档，但尚未实现 |
| **Regime 引擎** | 🟡 中 | 市场状态识别引擎（牛/熊/震荡），文档已设计但未实现 |
| **持仓引擎 (Position Engine)** | 🟡 中 | 管理当前持仓、计算盈亏、动态调仓 |
| **Binance WebSocket 完善** | 🟡 中 | 已有基础实现，需要完善并集成 |
| **统一数据源入口** | 🟡 中 | collectors/ 和 sources/ 两套架构，需要统一入口 |

### 🟢 P2 - 优化缺失（可选）

| 功能 | 影响 | 说明 |
|------|------|------|
| **清理重复代码** | 🟢 低 | `qq_adapter.py` 出现了 3 次，需要清理 |
| **测试文件整理** | 🟢 低 | 测试文件散落各处，建议统一到 `tests/` 目录 |
| **API 网关统一入口** | 🟢 低 | 各服务有独立 API，建议统一网关管理 |
| **系统集成测试** | 🟢 低 | 缺少端到端集成测试 |
| **文档完善** | 🟢 低 | 补充部署文档、API 文档、架构文档 |

---

## 四、架构一致性检查

### 4.1 ✅ 做得好的地方

1. **StandardEvent 协议**：统一事件格式，内部/外部合约分离清晰
2. **熔断/重试/降级**：完整的弹性基础设施
3. **实时源设计**：QQ/Telegram 实时源设计一致，共享 BaseSource 基类
4. **多服务架构**：服务拆分合理，职责清晰
5. **Kafka 消息总线**：消息流设计合理（raw_data → events → signals → orders）

### 4.2 ⚠️ 需要改进的地方

| 问题 | 现状 | 建议 |
|------|------|------|
| **两套数据采集架构** | collectors/ (定时) 和 sources/ (实时) | 保持两套，但统一入口和事件格式 |
| **重复代码** | qq_adapter.py 出现多次 | 清理历史遗留代码 |
| **EventBus 未集成** | 已实现但未使用 | 统一使用 EventBus 或 Kafka，建议优先 Kafka |
| **文档与实现不一致** | `doc/` 中有完整设计文档，但部分未实现 | 逐步实现设计文档中的功能 |
| **服务间集成** | 各服务独立，尚未完整连通 | 构建完整数据管道（source → event → signal → decision） |

---

## 五、下一步建议

### 5.1 第一阶段（1-2 周）：核心功能打通

1. **集成 QQ/Telegram 实时源到主流程**
   - 在 `data_service/main.py` 中启动实时源
   - 将实时源产生的 StandardEvent 发布到 Kafka

2. **实现 execution_service**
   - 交易所 API 对接（支持 Binance/OKX 等）
   - 订单管理（创建、查询、取消）
   - 风险管理前置检查

3. **实现 risk_service**
   - 仓位管理
   - 止损止盈
   - 最大回撤控制
   - 资金使用率控制

### 5.2 第二阶段（2-4 周）：策略完善

4. **完善 Event 提取**
   - 集成 llm_service 进行智能事件提取
   - 规则 + LLM 结合方式

5. **实现 Factor 系统**
   - 基础因子（RSI、MACD、布林带等）
   - 因子计算服务
   - 因子回测

6. **实现 Regime 引擎**
   - 市场状态识别
   - 自适应策略切换

7. **实现回测系统**
   - 历史数据回放
   - 策略回测评估
   - 绩效指标计算

### 5.3 第三阶段（1-2 周）：优化清理

8. **清理重复代码**
   - 删除重复的 qq_adapter.py
   - 整理测试文件

9. **完善集成**
   - 端到端数据管道测试
   - 性能优化
   - 错误处理优化

10. **文档完善**
    - API 文档
    - 部署文档
    - 架构文档更新

---

## 六、总结

### 6.1 总体评价

| 维度 | 评分 | 说明 |
|------|------|------|
| **架构设计** | 8/10 | 设计合理，分层清晰，协议统一 |
| **基础设施** | 9/10 | 熔断/重试/降级、Kafka、数据库等都很完善 |
| **数据服务** | 8/10 | 数据源丰富（QQ/Telegram 新增），但集成不完整 |
| **服务完整性** | 6/10 | 缺少执行、风控、回测等核心服务 |
| **可扩展性** | 9/10 | 模块设计良好，易于扩展 |

### 6.2 关键改进点

1. **打通数据流**：将新的实时源集成到主流程
2. **实现缺失服务**：execution_service、risk_service
3. **完善策略系统**：Factor、Regime、回测
4. **清理代码**：删除重复代码，整理测试

### 6.3 快速启动指南

现在系统可以运行，但仅能做**部分功能测试**：

```bash
# 测试 QQ 实时源（模拟模式）
python test_qq_source.py

# 测试 Telegram 实时源（模拟模式）
python test_telegram_source.py

# 运行 data_service API
cd backend
python -m services.data_service.main

# 运行 LLM 服务
python -m services.llm_service.main
```

**但无法完成完整交易流程**，因为缺少：
- execution_service（无法下订单）
- risk_service（无法做风控）
- 完整数据管道集成
